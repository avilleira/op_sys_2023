#include <stdio.h>
#include <stdlib.h>


void
usage (void) {
    fprintf(stderr, "usage: ./copybytes origin dest [N]\n");
    exit(EXIT_FAILURE);
}


int
main (int argc, int argv[]) {
    return EXIT_SUCCESS;
}