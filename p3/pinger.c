#include <stdio.h>
#include <stdlib.h>

void
usage(void) {
    fprintf(stderr, "usage: pinger N machine1 [machines ...]\n" );
    exit(EXIT_FAILURE);
}

int main (int argc, char *argv[]){
    return EXIT_SUCCESS;
}